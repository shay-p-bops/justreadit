{
  "name": "justreadit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A private, local-first Kokoro text-to-speech browser extension.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "node --check src/background.js && node --check src/content.js && node --check src/offscreen.js && node --check src/popup.js && node --check src/text.js && node --check scripts/build.mjs"
  },
  "dependencies": {
    "kokoro-js": "1.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
